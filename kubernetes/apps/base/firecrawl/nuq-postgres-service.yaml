apiVersion: v1
kind: Service
metadata:
  name: firecrawl-nuq-postgres
spec:
  type: ClusterIP
  selector:
    app: firecrawl-nuq-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
