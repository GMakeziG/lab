apiVersion: v1
kind: Service
metadata:
  name: forgejo-postgres
  labels:
    app: forgejo-postgres
spec:
  type: ClusterIP
  selector:
    app: forgejo-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
