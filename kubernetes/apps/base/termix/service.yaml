apiVersion: v1
kind: Service
metadata:
  name: termix
  labels:
    app.kubernetes.io/name: termix
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: termix
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
