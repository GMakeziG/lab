apiVersion: v1
kind: ServiceAccount
metadata:
  name: termix
  labels:
    app.kubernetes.io/name: termix
# Termix never calls the Kubernetes API, so the pod must not receive a token.
automountServiceAccountToken: false
