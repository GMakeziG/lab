apiVersion: v1
kind: ServiceAccount
metadata:
  name: trek
  labels:
    app.kubernetes.io/name: trek
# TREK never calls the Kubernetes API — server/package.json carries no
# Kubernetes client — so the pod must not receive a token.
automountServiceAccountToken: false
