apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallabag-postgres
  labels:
    app.kubernetes.io/name: wallabag-postgres
spec:
  # Single replica, PVC-backed, node-pinned: Recreate avoids two pods racing
  # for the same ReadWriteOnce local-path volume during a rollout.
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: wallabag-postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: wallabag-postgres
    spec:
      # local-path storage is node-local; pin to niner so the PV location is
      # deterministic and lands on the same node as the Wallabag PVCs. This is a
      # single point of failure by design -- no replication, no failover.
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - niner
      securityContext:
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
      containers:
        - name: postgres
          image: postgres:17.11-alpine
          imagePullPolicy: IfNotPresent
          env:
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: wallabag-secret
                  key: POSTGRES_PASSWORD
            # No POSTGRES_DB is set; the Wallabag entrypoint creates the
            # "wallabag" database and user on first boot using the superuser
            # credentials.
            # initdb requires the data directory to be 0700, which it cannot
            # enforce on the fsGroup-owned mount root. Use a subdirectory.
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - name: postgres
              containerPort: 5432
              protocol: TCP
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: wallabag-postgres-data
