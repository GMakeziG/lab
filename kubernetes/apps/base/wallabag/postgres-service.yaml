apiVersion: v1
kind: Service
metadata:
  name: wallabag-postgres
  labels:
    app.kubernetes.io/name: wallabag-postgres
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wallabag-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
