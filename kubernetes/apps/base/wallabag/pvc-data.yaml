apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wallabag-data
  labels:
    app.kubernetes.io/name: wallabag
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
