apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: observability
  namespace: flux-system
spec:
  dependsOn:
    - name: platform
  interval: 10m
  path: ./kubernetes/observability
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
